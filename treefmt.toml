# Formatting for this repo, used both by `treefmt` on the CLI and by
# tailorED itself on save (the editor's single formatting integration).

[formatter.rustfmt]
command = "rustfmt"
options = ["--edition", "2024"]
includes = ["*.rs"]

[formatter.nixfmt]
command = "nixpkgs-fmt"
includes = ["*.nix"]
